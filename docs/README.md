# Screenshots

Put these two images here so the main README can display them:

1. **screenshot-analyze.png** – Terminal output of `archsketch analyze samples/fullstack-app` (full output: diagram, legend, table).
2. **screenshot-export-svg.png** – The exported architecture diagram (e.g. open `arch.svg` in a browser and capture the window).

Then the links in the root README will work.
